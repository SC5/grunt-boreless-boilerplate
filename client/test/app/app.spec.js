define(['jquery', 'app'], function($, app) {
	// App bootstrapping tests
	describe('Application', function() {
		// TODO: Write the backbone tests
		
		// Cleanup
		var statusText = 'This is the body text to be replaced';
		beforeEach(function() {
		});
		
		it('does nothing', function() {
		});
	});
});