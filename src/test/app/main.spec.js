define(['app'], function(app) {
	// App bootstrapping tests
	describe('Application', function() {
		
		// Cleanup
		beforeEach(function() {
		});
		
		it('does nothing real', function() {
			app.start();
		});
	});
});